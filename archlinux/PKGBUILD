pkgname=gstatus
pkgver=0.1
pkgrel=1
pkgdesc="An i3bar plugin that displays the remaining battery percentage and time."
arch=('x86_64')
license=('GPL')

build() {
	make -C "$startdir/.."
}

package() {
	install -D "$startdir/../gstatus" "$pkgdir/usr/bin/gstatus"
}
